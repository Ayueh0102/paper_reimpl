# 03 Stage A v2 — 200k step warm-start from v1 ckpt.
seed: 42
batch_size: 4
num_workers: 0
base_learning_rate: 4.5e-6
weight_decay: 0.01
accumulate_grad_batches: 1
max_epochs: 1000
max_steps: 200000
log_every: 2000
ckpt_dir: papers/03_if_font/outputs/stage_a_ttf_v2
# Pretrained VQGAN (our own, 30k step on 13 OFL TTF). Replaces the random-init
# stub adapter that caused sq=0 codebook collapse in the earlier v2 attempt.
vqgan_local_path: papers/03_if_font/outputs/stage_vqgan/vqgan_last.pt
