# 08 Stage A v2 — 200k step warm-start from v1.
seed: 42
batch_size: 16
num_workers: 0
learning_rate: 1.0e-4
weight_decay: 0.0
grad_clip: 1.0
max_epochs: 1000
max_steps: 200000
log_every: 2000
cfg_drop_prob: 0.1
pinn_weight: 0.0
ckpt_dir: papers/08_dp_font/outputs/stage_a_ttf_v2

diffusion:
  timesteps: 1000
  beta_start: 1.0e-4
  beta_end: 2.0e-2
  beta_schedule: cosine
  prediction_target: epsilon
