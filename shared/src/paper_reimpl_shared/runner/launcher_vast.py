"""Vast.ai launcher (stub).

Future: emit Dockerfile + onstart.sh + B2 snapshot pull. For now this is a
placeholder so the architecture's `--data-backend vast_snapshot` path
resolution works.
"""

from __future__ import annotations


def stub() -> None:
    raise NotImplementedError(
        "Vast.ai launcher not implemented yet. See docs/VAST_BACKEND.md for plan."
    )
